<!DOCTYPE html>
<html lang="en">
	<head>
		<title>TinyApp</title>
		<link rel="stylesheet" type="text/css" href="../style.css">
	</head>
	<body>
    <% include partials/_header %>
	  <section>
	  	<form>
		    <a href="/urls/new">Create your shortUrl!</a>
	  	</form>
		  <% for ( var key in urls ) { %>
			  <form method="POST" action="/urls/<%= key %>/delete">
					<p><%= key %> -> <%= urls[key].longUrl %></p>
					<p>
						<a href="/urls/<%= key %>">Edit</a>
						<button type="">Delete</button>
					</p>
			  </form>
		  <% } %>
	  </section>
	</body>
</html>